{
  "name": "amelienergie-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n amelienergie,pompihouse -c blue,magenta \"npm run dev --prefix apps/amelienergie\" \"npm run dev --prefix apps/pompihouse\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
